# AI Agent Task Platform 协作规则

## 语言

- 当前项目的文档、教学说明和代码注释使用中文。
- Python 标识符、HTTP 字段、数据库字段和第三方库名称使用规范英文。
- 不在代码中堆叠无意义注释；注释重点解释事务、并发、Session 生命周期和安全边界。

## 实施节奏

- 项目按四个阶段推进，每个阶段独立实现、验证和汇报。
- 未获得用户对下一阶段的明确许可前，不提前实现后续阶段。
- 修改前先说明目标、方案、验证方式和风险边界。

## 工程约束

- API 层只负责 HTTP 输入输出和依赖注入。
- Service 层负责业务规则与事务边界。
- Repository 层负责 SQLAlchemy 2.x 数据访问，不使用 `session.query()`。
- 数据库结构只通过 Alembic 管理，生产环境禁止使用 `Base.metadata.create_all()`。
- 后台任务不得复用请求结束后会被关闭的数据库 Session。
- 运行任务必须通过原子条件更新防止重复执行。

## 质量要求

- 所有公开函数尽量提供类型注解。
- 配置必须来自环境变量，不提交 `.env` 或真实密钥。
- 每个阶段至少执行语法或导入检查；具备依赖和数据库时再执行集成测试。
- 保留用户已有改动，不执行破坏性 Git 操作，不自行提交、推送或创建 PR。
